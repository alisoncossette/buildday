Make `python grade.py` exit 0 with every required check green:

A voice agent speaks AS the owner (cloned voice) ONLY within a live Bolo grant, and the instant
consent is revoked it SHIFTS to a generic voice — zero clone utterances after revoke, every
transition audited — without a human writing any verdict the rubric measures.
