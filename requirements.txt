# Stead app server (deploy) — what app/server.py needs to run in the cloud.
psycopg[binary]      # Neon Postgres (the shared consent brain + care timeline)
anthropic            # "Ask Stead" + doctor Q&A
httpx                # outbound Vapi calls (agent/tools/phone.py)
python-dotenv        # local .env loading (no-op in cloud where envs are set in the dashboard)
composio   # Mom/Ruby calendar+email via /connect.html (managed OAuth)
